class ApiConstants {
  /// Base URL for the backend API.
  /// 
  /// In an Android emulator, 10.0.2.2 resolves to the host machine's localhost.
  /// This expects the backend to be exposed on port 8080 on the host machine.
  static const String baseUrl = 'https://vitable-backend-dep6b6yxta-uc.a.run.app';
}
